DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=127.0.0.1,localhost
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Default production/local database engine
DB_ENGINE=django.db.backends.mysql
DB_NAME=heartify_lms
DB_USER=root
DB_PASSWORD=root
DB_HOST=127.0.0.1
DB_PORT=3306

# Django uses mysqlclient/MySQLdb as the MySQL Python driver.
